# 2. 알고리즘 (의사 코드) (10점)

## 1. `main` 함수의 전체 흐름
(프로젝트의 전체 작동 로직을 의사 코드로 작성하세요.)

(예시 - 관리 프로그램의 경우)
```
시작
반복 (사용자가 '종료'를 선택할 때까지):
    메뉴 출력 (1.영상등록, 2.영상 목록 보기, 3.평균 평점 보기 0.종료)
    사용자로부터 메뉴 션택 입력 받기
    만약 선택이 1이면:
        영상 등록 함수 호출
    만약 선택이 2이면:
        영상 목록 출력 함수 호출
    만약 선택이 3이면:
        평균 평점 계산 함수 호출
    만약 선택이 0이면:
        "프로그램을 종료합니다" 출력
        반복 종료
반복 끝
종료
```

## 2. 핵심 기능 1: 영상 등록 함수
```
(핵심 기능 1의 논리적 절차를 작성하세요.)

시작
    만약 영상 수가 최대 저장 개수를 초과하면:
        "등록 불가" 메시지 출력
        함수 종료
    아니면:
        영상 제목 입력받기
        영상 장르 입력받기 (다큐, 옌능, 영화)
        영상 길이(분 단위)입력받기
        영상 정보를 배열에 저장
        현재 저장된 영상 수 1증가
종료
```

## 3. 핵심 기능 2: 영상 추천 함수
```
(핵심 기능 2의 논리적 절차를 작성하세요.)
```
시작
    사용자의 선호 장르 입력받기
    배열에서 해당 장르와 일치하는 영상 탐색
    일치하는 영상이 있다면:
        영상 제목과 길이 출력
    일치하는 영상이 없다면:
        "추천할 영상이 없습니다."출력
종료